# ============================================================
#  personal_blog — Environment Variables
#  1. Copy this file:  cp .env.example .env
#  2. Fill in values below
#  3. NEVER commit .env to git (it's in .gitignore)
# ============================================================

# Flask
# Generate: python -c "import secrets; print(secrets.token_hex(32))"
FLASK_SECRET_KEY=replace_with_a_long_random_string

# SMTP — Gmail
# Use an App Password, NOT your login password.
# Create one at: https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=kiranhl2003@gmail.com
SMTP_PASSWORD=vswf wmug svmz ahrf

# Where contact form emails land
RECIPIENT_EMAIL=kiranhl1709@gmail.com

# CORS — set to your deployed domain in production
# Example: https://kiranhl.dev
ALLOWED_ORIGIN=*
